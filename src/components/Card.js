import React from 'react';
import {View } from 'react-native';

const Card = () => {
  return (
    <View></View
  );
};

const styles {
  containerStyle:{
    borderWidth: 1,
    borderWidth: 2,
    borderColor: '#ddd',
    borderBottomWidth: 0,
  }
};

export default Card
